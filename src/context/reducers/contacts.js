const contacts = (state , {type,payLoad})=>{

    switch(type){
        case 'GET_CONTACT':
            return state;
            break;
        default:
            return state;
        
    }

}

export default contacts;